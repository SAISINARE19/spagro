import { EnquiryForm } from "@/components/enquiry-form";export const metadata={title:"Request a Quote"};export default async function Quote({searchParams}:{searchParams:Promise<{product?:string}>}){const {product}=await searchParams;return <main className="shell" style={{paddingTop:"4rem"}}><p className="eyebrow">Request a quote</p><h1 className="display" style={{fontSize:"clamp(2.8rem,6vw,5rem)",margin:".5rem 0 1rem"}}>Tell us what you need.</h1><p style={{maxWidth:600,lineHeight:1.7,color:"#59665d"}}>Share your requirement and any drawing or reference image. S.P. AGRO will review it and follow up.</p><div style={{marginTop:26}}><EnquiryForm product={product}/></div></main>}
