import { GalleryCard } from "@/components/catalogue-cards";import { getGallery } from "@/lib/catalogue";export const metadata={title:"Gallery"};export default async function Gallery(){const items=await getGallery();return <main className="shell" style={{paddingTop:"4rem"}}><p className="eyebrow">S.P. AGRO gallery</p><h1 className="display" style={{fontSize:"clamp(2.8rem,6vw,5rem)",margin:".5rem 0 2rem"}}>Work in focus.</h1>{items.length?<div className="card-grid">{items.map(i=><GalleryCard item={i} key={i.id}/>)}</div>:<div className="surface" style={{padding:"2rem"}}>No gallery images available yet.</div>}</main>}
