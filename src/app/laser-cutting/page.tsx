import { LaserCard } from "@/components/catalogue-cards";import { EnquiryForm } from "@/components/enquiry-form";import { getLaserProjects } from "@/lib/catalogue";
export const metadata={title:"Laser Cutting Services"};export default async function Laser(){const projects=await getLaserProjects();return <main><section style={{background:"#202420",color:"white",padding:"5rem 0"}}><div className="shell"><p className="eyebrow">S.P. AGRO services</p><h1 className="display" style={{maxWidth:800,margin:".6rem 0 1rem"}}>Precision laser cutting services.</h1><p style={{maxWidth:600,lineHeight:1.7,color:"#cbd3ca"}}>Accurate cutting for agricultural machinery, industrial components and custom metal designs.</p></div></section><section className="shell" style={{padding:"5rem 0"}}><div className="card-grid">{["High Precision","Clean Edges","Custom Shapes","Consistent Results","Small & Large Batches","Custom Requirements"].map((x,i)=><div className="surface" style={{padding:"1.3rem"}} key={x}><span className="eyebrow">0{i+1}</span><h2 style={{fontSize:"1.1rem"}}>{x}</h2></div>)}</div></section><section className="shell"><p className="eyebrow">Portfolio</p><h2 className="section-title">Our laser work.</h2>{projects.length?<div className="card-grid" style={{marginTop:24}}>{projects.map(p=><LaserCard key={p.id} project={p}/>)}</div>:<div className="surface" style={{padding:"2rem",marginTop:24}}>No laser projects have been added yet.</div>}</section><section className="shell" style={{paddingTop:"5rem"}}><p className="eyebrow">Start a project</p><h2 className="section-title">Request a laser cutting quote.</h2><div style={{marginTop:22}}><EnquiryForm type="laser"/></div></section></main>}
