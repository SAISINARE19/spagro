import Link from "next/link";import { EnquiryForm } from "@/components/enquiry-form";import { getSettings } from "@/lib/catalogue";import { getWhatsAppUrl } from "@/lib/whatsapp";export const metadata={title:"Contact"};export default async function Contact(){const s=await getSettings();return <main className="shell" style={{paddingTop:"4rem"}}><p className="eyebrow">Contact S.P. AGRO</p><h1 className="display" style={{fontSize:"clamp(2.8rem,6vw,5rem)",margin:".5rem 0 2rem"}}>Let&apos;s discuss your requirement.</h1><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(290px,1fr))",gap:32}}><div className="surface" style={{padding:"1.6rem"}}><h2>Visit or contact</h2><p style={{whiteSpace:"pre-line",lineHeight:1.7}}>{s.address}</p>{s.phone&&<p><b>Phone:</b> <a href={`tel:${s.phone}`}>{s.phone}</a></p>}{s.email&&<p><b>Email:</b> <a href={`mailto:${s.email}`}>{s.email}</a></p>}{s.business_hours&&<p><b>Hours:</b> {s.business_hours}</p>}<div style={{display:"flex",gap:10,flexWrap:"wrap"}}><Link href={getWhatsAppUrl(s.whatsapp,"Hello S.P. AGRO, I would like to know more.")} className="button button-accent">WhatsApp</Link>{s.maps_url&&<Link href={s.maps_url} className="button button-outline">Get directions</Link>}</div></div><EnquiryForm type="contact"/></div></main>}
